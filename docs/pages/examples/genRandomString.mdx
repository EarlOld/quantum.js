# `genRandomString` Method

## Description

This method generates a random string of a given length.

## Parameters

- `length` - The length of the string to generate.

## Return Value

A string of the given length.

## Example

```typescript
const randomString = Circuit.genRandomString(5);
console.log(randomString); // "aBcDe
```