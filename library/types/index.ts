export type ObjectQubitState = {
  index: number;
  indexBinStr: string;
  amplitude: { re: number; im: number };
  amplitudeStr: string;
  magnitude: number;
  chance: number;
  chanceStr: string;
  phase: number;
  phaseStr: string;
};
